extern itoa(num): str
extern println(str): void

fun f(n: num): num {
    if n < 2 return n
    return f(n - 1) + f(n - 2)
}

pub fun main(): num {
    println(
        itoa(
            f(30)
        )
    )
    return 0
}