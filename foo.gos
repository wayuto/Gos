$import "gosio"
$import "convert"

fun f(n: flt): flt {
    return n * 2.5
}

pub fun main(): int {
    let n: flt = atof(
        input("Enter a number: ")
    )
    println(
        ftoa(
            f(n)
        )
    )
    return 0
}
