extern range(num num): arr<_>

pub fn find(a: arr<_> e: num): num {
    let i: num = 0
    while i < sizeof a {
        if a[i] == e {
            return i
        }
        i++
    }
    return (-1)
}