$import "gosio"
$import "convert"

fun fib(n: num a: num b: num): num {
    if n == 0 return a
    return fib(n - 1 b a + b)
}


pub fun main(): num {
    let n: num = fib(1000 0 1)
    println(
        itoa(
            n
        )
    )
    return 0
}